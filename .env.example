# Google Apps Script Web App URL for email subscriptions
# See README for setup instructions
Subscribers=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# POWER CUT 10%-code popup (/powercut). Optional. Email only.
# POWERCUT_LEADS: dedicated Apps Script / Zapier catch-hook URL that accepts
#   { email, ... }. If unset, the popup falls back to the Subscribers URL above.
# POWERCUT_LEADS=https://hooks.zapier.com/hooks/catch/XXXX/YYYY/
#
# The discount code shown to the visitor and returned by the API. Must match a
# real 10%-off coupon you create in your store. Defaults to PEAKSTATE.
# POWERCUT_CODE=PEAKSTATE
# NEXT_PUBLIC_POWERCUT_CODE=PEAKSTATE   # optional: lets the client render it instantly

# Peak State Creator Program application (/creator/apply → /api/creator-apply).
# Zapier Catch Hook URL that receives the full application payload and forwards
# it into ClickFunnels 2.0, where the "Creator Program" tag is applied. The API
# route has a placeholder fallback that intentionally fails until this is set.
# ZAPIER_CREATOR_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/XXXX/YYYY/

# Stripe secret key (server-side only — never expose to the client).
# Used by /api/create-checkout-session to create Checkout Sessions.
STRIPE_SECRET_KEY=sk_test_...

# Optional. Absolute origin used to build success_url / cancel_url. If
# unset, the API derives the origin from the request headers, which works
# on Vercel and locally without extra config. Set this when running behind
# a proxy where the incoming Host header doesn't reflect the public URL.
# NEXT_PUBLIC_SITE_URL=https://peakstate.shop
